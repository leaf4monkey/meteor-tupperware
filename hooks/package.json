{
  "name": "tupperbuild",
  "version": "1.0.0",
  "description": "Dockerfile ON BUILD script for meteor-builder",
  "main": "pre_build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "async": "^0.9.2",
    "lodash": "^3.8.0"
  }
}
